<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
  <tr>
    <td colspann="2" class="field">
      <%= f.label :name %><br>
       <%= f.text_field :name %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <%= f.label :strasse %><br>
      <%= f.text_field :strasse %>
    </td>
    <td class="field">
      <%= f.label :hnr %><br>
      <%= f.text_field :hnr %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <%= f.label :plz %><br>
      <%= f.text_field :plz %>
    </td>
    <td class="field">
      <%= f.label :ort %><br>
      <%= f.text_field :ort %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <%= f.label :tel %><br>
      <%= f.text_field :tel %>
    </td>
    <td class="field">
      <%= f.label :email %><br>
      <%= f.text_field :email %>
    </td>
  </tr>
  <tr>
    <td colspann="2" class="actions">
     <%= f.submit %>
    </td>
  </tr>
  </table>
<% end %>
