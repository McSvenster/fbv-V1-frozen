<article class="kommendetage">
  <% @range.each do |d| %>
    <% if d == Date.today || @dailybookings[d] < 1 %>
      <%= render "tag", :d => d %>
    <% else %>
      <a href=<%= new_booking_path(datum: d) %>><%= render "tag", :d => d %></a>
    <% end %>  
  <% end %>
</article>