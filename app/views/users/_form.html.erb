<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
  <tr>
    <td colspann="2" class="field">
      <%= f.label "E-Mail (= Login)" %><br>
      <%= f.text_field :email %>
    </td>
  </tr>
  <tr>
    <td colspann="2" class="field">
      <%= f.label "Firma" %> (oder erst <%= link_to "neue Firma anlegen?", new_company_path %>)<br>
      <%= f.collection_select(:company_id, Company.all, :id, :name) %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <%= f.label :vname %><br>
      <%= f.text_field :vname %>
    </td>
    <td class="field">
      <%= f.label :nname %><br>
      <%= f.text_field :nname %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <%= f.label :strasse %><br>
      <%= f.text_field :strasse %>
    </td>
    <td class="field">
      <%= f.label :hnr %><br>
      <%= f.text_field :hnr %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <%= f.label :plz %><br>
      <%= f.text_field :plz %>
    </td>
    <td class="field">
      <%= f.label :ort %><br>
      <%= f.text_field :ort %>
    </td>
  </tr>
  <tr>
    <td class="field">
      <%= f.label :tel %><br>
      <%= f.text_field :tel %>
    </td>
    <td class="field">
      <%= f.label :role %><br>
      <%= f.select :role, ([["Planer", 2], ["Bestatter", 3]]) %>
    </td>
  </tr>
  <tr>
    <td class="actions">
      <%= f.submit %>
    </td>
    <td>
      <% unless current_page?(new_user_path) %>
        <%= link_to 'Bestatter / Planer löschen', @user, method: :delete, data: { confirm: 'Sind Sie sicher?' }, style: "color:darkred;" %>
      <% end %>
    </td>
  </tr>
  </table>
<% end %>
